console.log(navigator);

// Get information and display it by console.log
const info = {
  system: navigator.platform,
  lang  : navigator.languages,
  location : navigator.geolocation,
};
console.log(info);

// List plaguin
